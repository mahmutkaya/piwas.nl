# Public URL of the site — used for sitemap, robots, OG tags and checkout links.
NEXT_PUBLIC_SITE_URL=https://piwas.nl

# This site is intentionally not a checkout (see /legal/terms). Payments for
# Piwas products run inside each product (Domainio uses Mollie; SofraPiwas
# uses Stripe — enable TWINT there if you want Swiss TWINT billing).
